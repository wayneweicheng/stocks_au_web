# SQL Server connection
sql_server_host=
sql_server_port=1433
sql_server_database=
sql_server_user=
sql_server_password=

# Trading Signal admin catalogue query timeout (seconds)
TRADING_SIGNAL_ADMIN_QUERY_TIMEOUT=60

# Support/resistance database safeguards (seconds / request slots)
SUPPORT_RESISTANCE_DB_CONNECTION_TIMEOUT=5
SUPPORT_RESISTANCE_DB_QUERY_TIMEOUT=8
SUPPORT_RESISTANCE_MAX_CONCURRENT_REQUESTS=1

# Frontend origin(s) for CORS, comma-separated
allowed_origins=http://localhost:3100

# SPXW GEX signal assistant
SPX_GEX_STRATEGY_ENABLED=true
SPX_GEX_SOURCE_DATABASE=StockDB_US
# sql uses StockDB_US.Transform.OptionGEXChangeCapitalType; file is useful for backtests
SPX_GEX_DATA_MODE=sql
SPX_GEX_GEX_PATH=data/option_gex_delta_signal_SPXW_2025-01-01_to_present.csv
SPX_GEX_NQ_PATH=data/NQMain_30M.csv
SPX_GEX_DB_PATH=data/spx_gex_strategy.sqlite3
SPX_GEX_TIMEZONE=America/New_York
SPX_GEX_LOOKBACK_DAYS=60
SPX_GEX_INITIAL_CAPITAL=100000
SPX_GEX_EXPOSURE_FACTOR=1.0
SPX_GEX_REQUIRE_LIVE_NQ=true
SPX_GEX_NOTIFICATION_NO_SIGNAL=false
# Set this to the URL reachable from your phone; Pushover adds it as a button.
SPX_GEX_REPORT_URL=https://pegasus.asxstocktoolings.com.au/api/spx-gex/report.html
# Optional shared token for the read-only HTML report endpoint.
SPX_GEX_REPORT_TOKEN=

# Pushover secrets: fill these locally; never commit them.
PUSHOVER_USER_KEY=
# Application token used by the existing generic notification handler.
PUSHOVER_APP_TOKEN=
# Legacy alias also accepted by this backend:
# PUSHOVER_API_TOKEN=
# Optional; matches the existing mobile helper defaults.
PUSHOVER_DEVICE=droid4
PUSHOVER_SOUND=echo

# IB continuous NQ contract used for the live NQMAIN quote.
IB_NQ_SYMBOL=NQ
IB_NQ_EXCHANGE=CME
IB_NQ_CURRENCY=USD
IB_MARKET_DATA_TYPE=1

# Authentication credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123

# Charts base directory (local path to prefix ChartPath when serving via /charts)
# Examples:
#   CHART_BASE_DIR=C:\\pattern_charts\\
#   CHART_BASE_DIR=\\\\fileserver\\charts\\
# Backward compatible key names: CHART_BASE_DIR or CHART_BASE_URL
CHART_BASE_DIR=

# IB Gateway automation
# Path to IB Gateway executable (e.g., C:\IB\IBGateway\1013\ibgateway.exe)
# Only required if not using IBC integration
IBG_EXE_PATH=
# Credentials used to auto-fill login form (only needed if not using IBC)
IBG_USERNAME=
IBG_PASSWORD=
# Seconds to wait after killing existing IBG instance before restart
IBG_WAIT_AFTER_KILL_SECONDS=10

# IBC Integration (RECOMMENDED for unattended/automated operation)
# IBC (IBController) is the industry-standard solution for automating IB Gateway login
# Advantages:
#   - Works without active desktop session (no UI Automation required)
#   - More reliable than screen automation
#   - Handles login automatically using IBC's config.ini
# Setup:
#   1. Install IBC from https://github.com/IbcAlpha/IBC
#   2. Configure credentials in IBC's config.ini (usually C:\Users\<user>\Documents\IBC\config.ini)
#   3. Set IBG_USE_IBC_SCRIPT=true and IBG_IBC_SCRIPT_PATH to StartGateway.bat location
# If false, uses direct launch with UI Automation (requires active desktop)
IBG_USE_IBC_SCRIPT=false
IBG_IBC_SCRIPT_PATH=

# IB API status probe (host/port). If set, status checks this exact endpoint.
# Example: IBG_API_HOST=192.168.20.102
#          IBG_API_PORT=7496
IBG_API_HOST=
IBG_API_PORT=

# IB Gateway UI preferences
# IBG_TRADING_MODE=Live or Paper
IBG_TRADING_MODE=Live

# Optional: enable a foreground typing fallback when UIA automation fails
# Set to true only if you understand the risks of sending keystrokes to the foreground window.
# Default is false for safety.
IBG_ALLOW_FALLBACK_TYPING=

# Optional calibration (relative window coords 0..1). If set, automation clicks these exact points.
# Example: 0.52 means 52% of window width from left; 0.45 means 45% of height from top.
IBG_USERNAME_X_PCT=
IBG_USERNAME_Y_PCT=
IBG_PASSWORD_X_PCT=
IBG_PASSWORD_Y_PCT=
